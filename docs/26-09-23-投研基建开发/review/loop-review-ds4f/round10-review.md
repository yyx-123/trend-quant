# Round 10 审查报告（loop-review-ds4f）——确认轮 5

> **状态：CLOSED（2026-09-25 闭合）**
> - 修复清单与验收记录：`round10-fixes.md`；
> - 全量回归（修复 + lint 收口后终跑）：**1652 passed / 2 failed**（均为既有 Windows flake）；
> - ruff：(file, rule) 集合与基线完全一致（新增 0 条）。
>
> 日期：2026-09-25
> 审查对象：commit `93a5583`（Round 9 结构性收口后的 HEAD）
> 审查方式：两个独立确认代理（R10A/R10B）。R10A 继续沿退化腿类做"**任一比值指标**"
> 的穷举与**变异实证**（闸门改坏后测试是否变红）；R10B 独立复检 Round 9 的收口与回归面。

## 总体结论

**R10B：CLEAN。R10A：NOT CLEAN（2 项 P2/P3 缺口 + 1 项 P3 过拦截/文案失真 + 3 条空钉）。**

这是退化腿类的**第 7 次复发**，但形态与前几次不同：前几次是"**新增消费面**"，这次是
"**同一消费点上判据的键位不全**"——闸门只看 `sharpe`，而 `sortino` 的分母是
"负收益子集 std"，与 Sharpe **不同源**，所以"Sharpe 正常、Sortino 1e5~1e14"的腿真实存在
并且已经落库。

---

## P2

### R10-F1 `sortino` 逃逸闸门（分母不同源）

- 事实（真实落库数据）：**实测 5 日窗口 → sortino = 1.58e5**，被写进
  `research_verdicts.report_json`、物化 `report.json`，并经 HTTP/MCP 对外。
- 修复：`is_degenerate_summary` 改为检查摘要里**任一比值型指标**
  （`_RATIO_METRIC_KEYS = ("sharpe", "sortino")`）超闸；**刻意不含 `calmar`**
  （低回撤/短窗口上可合法 > 50，纳入会误杀诚实策略）。

## P3

### R10-F2 `information_ratio` / `tracking_error` 未过闸

- 事实：两条近失配腿 → **IR = 1.4e13**。
- 修复：与 `beta`/`alpha`/`capture` 同闸。

### R10-F3 过拦截与文案失真

- 事实：5 日窗口的**合法** |年化 Sharpe| 上限实测 **49.98**（对闸门 50 只有 **1.00×** 余量），
  10 条**有成交**的真实短窗口腿被判退化，并被落上"**零成交或全现金**"的错误解释。
- 修复：文案改为"该腿的 Sharpe/Sortino/PSR/DSR 不可用（零成交/全现金、**近零方差或
  极短窗口**等）"；"窗口长度 vs 闸门口径"进待决策清单（见 `round10-fixes.md`）。

### 三条空钉（R10A 变异实证）

| # | 空钉形态 | 变异证据 | 修复 |
|---|---|---|---|
| 1 | 滚动闸门钉的夹具**方差恰为 0** | 删掉闸门后测试仍绿（空钉） | 夹具改为"极小的**非零**抖动"，并**断言未加闸路径确实产出噪声** |
| 2 | h2h 的年化闸门只有文本断言 | 改实现不改文案仍绿 | 改**载荷级**断言 |
| 3 | PBO 的年化闸门同上 | 同上 | 改**载荷级**断言 |

## 代理同时确认的正面结论

- R10B：Round 9 的结构性入口在端到端路径上成立（真 pipeline 零差异），回归面无漂移。
- 闸门在真实数据上的过拦截余量（Round 9 的 4.37 上限）在接入 sortino 后由 R10A 复核，
  短窗口（5 日）的余量被量化到 1.00×——**这是本轮唯一进入待决策的量化边界**。

## 闭合

修复见 `round10-fixes.md`，提交 `ccf52c7` + `1ce4f1b`（lint 收口）；
回归 1652 passed / 2 failed；新增钉子 3 项（空钉补强）。
