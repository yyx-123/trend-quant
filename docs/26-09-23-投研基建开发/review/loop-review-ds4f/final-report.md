# loop-review-ds4f 最终报告（投研基建 + 存量栈多轮审查）

> 状态：**Round 12 进行中**（本文件在 Round 12 闭合后补最后一节）
> 审查范围：`docs/26-09-20-投研基建架构/` 设计的实现（基线 `f93031e` → 本循环终点）
> 审查方式：每轮 ≥2 个独立对抗代理（本会话累计 **33 个**）+ 主审人逐条复核；修复一律配"钉子"，
> 并用**语义变异**证明钉子真的会变红（累计 100+ 个变异，其中 R11C 单轮 56 个）。
> 全部工作报告：`round1-review.md` … `round11-review.md` + 对应 `-fixes.md`。

---

## 一、一句话结论

**实现本身在数值上现在是对的**（复权基准、引擎一致性、退化腿噪声、ETF 报价口径、
旧引擎链闸门均已修复并通过独立复算/真实数据对拍验证），**但你还有 3 件事必须决定**
（见第五节）：生产口令与 TLS（安全）、生产服务重启（应用侧修复尚未生效）、
生产库 147 条历史噪声的处置。

## 二、轮次与结论

| 轮次 | 审查对象 | 结论 | 该轮提交 |
|---|---|---|---|
| R1 | `c986ad1` 投研基建一期全量 | 4 P1 + 12 P2 + 21 P3 | `606e86f`、`82459c6` |
| R2 | R1 的修复代码 | 1 P1 + 2 P2 + 9 P3（V3/V4 双 FAIL → 改精确恒等式 → V5 PASS） | `c1226c2`、`0285f98`、`0a6bfe7` |
| R3 | 平台/纪律/通道层 | 4 P2 + 10 P3（V7/V8 双 FAIL → V9 PASS） | `3a5f7d6`、`2fad537`、`692410c`、`90e48f8` |
| R4 | 数据一致/并发/UI/验收脚本 | 1 P1 + 1 P2 + 6 P3（V10 FAIL → V11 PASS） | `f590c2a`、`ebcd0f6` |
| R5 | 存量栈与跨栈原语 | 1 P1 + 3 P2 + 3 P3 | `f89c913` |
| R6 | 确认轮（复核 R5 修复） | 1 P1（**我引入的回归**）+ 1 P2 + 3 钉子缺口 | `cd09ddc` |
| R7 | 确认轮（退化腿第 4 次显形） | 3 P2 + 3 P3 | `8a5aca2` |
| R8 | 确认轮（第 5 次复发） | R8B CLEAN / R8A：2 P2 + 3 P3 | `eb1650c` |
| R9 | 确认轮（第 6 次复发） | 2 P2 + 1 P3 | `93a5583` |
| R10 | 确认轮（第 7 次复发） | R10B CLEAN / R10A：1 P2 + 2 P3 + 3 空钉 | `ccf52c7`、`1ce4f1b` |
| — | 审查痕迹清理（用户要求） | 生产代码 220 处编号 + 常量副本 + `ast.Invert` | `74443ad` |
| R11 | 确认轮（2 个新机制面 + 第 8 次复发） | **3 P1** + 2 P2 + 4 P3 + 6 空钉 | `f023cfb` |
| R12 | 对抗验证 R11 修复 + 存量面新审 | **6 P2**（退化类第 9 次复发 / 导出跨用户泄露 / 实盘清单三处 / 旧栈批量缺冻结门） | 见第七节 |
| R13 | 证伪 R12 修复 + 残余未闸面穷举 | **3 P2**（全部是"上一轮声称已修但未闭合"：影子现金只改一半 / CLI 冻结门不跨进程 / backfill 修复从未落地 + 格子列读取面无闸） | `17d9aea` |
| R14 | 证伪 R13 修复 + 同源口径五面扫 | **R14A CLEAN / R14B 2 P2**（冻结门缺写入侧 / 实盘清单无用户维度） | `c0f5e66` |
| R15 | 证伪 R14 修复 + 材质性缺陷专项（A 股规则/费用/守恒/结论/时区） | **1 P1 + 2 P2**（创业板涨跌停幅度无日期维度 → 13 笔不可达成交；对账写回缺 user_id；写入侧守卫未下沉） | `2533deb` |
| R16 | 品种级交易规则专项 + 证伪 R15 修复 | **R16B CLEAN / R16A 1 P2 + 1 P3**（科创板最小申报 200 股未实现 → 产出必被拒的 100 股"成交"；2023-08-10「100+1」未建模） | `ac0dd7e` |
| R17 | 证伪 R16 修复 + 出口数字一致性审计 | **R17B CLEAN / R17A 2 P2**（quantity 路径现金递减后未复检最小申报〔已提交态存在〕；**旧栈 `rule_backtest` 是第二个下单入口** → 生产库 336 笔 100 股科创板"成交"） | `8eee575` |
| R18 | 证伪 R17 修复 + 研究结论可信度专项（统计判据） | **3 P2**（旧栈基准腿仍按 100 股为科创板建仓；plateau 判据在 <3 邻域点时双向失效；**confirmed 门的 DSR 条件恒真 → 平台无多重检验折扣**） | `c89bb1f` |

## 三、所有 P1（会改数字/结论/安全）与修复状态

| # | 问题 | 影响 | 修复 | 状态 |
|---|---|---|---|---|
| R1-P1-1 | 可交易性除权基准晚一根 bar 生效 | 274 只标的 2015 起 467 天假涨停 + 465 天假跌停（引擎据此拒买/阻塞止损） | 因子改落到登记日**之后**第一根 bar，支持同标的多因子累乘 | ✅ 完成（真实库 107/107 实证） |
| R1-P1-2 | holdout 窗口按字符串比较 | 改个日期格式即可绕过 holdout 卡控 | 严格 ISO 解析 + fail-closed | ✅ 完成 |
| R1-P1-3 | `bucket_analysis` 读持仓崩溃 | 实验 failed 且污染 DSR 试验计数 | 共享空账户桩 | ✅ 完成 |
| R1-P1-4 | 组合报告 turnover 被除两次平均权益 | "假 0 换手" | 成交额与比率分离 | ✅ 完成 |
| R2A-P1-1 | parity 归因上限在生产尺度发散 | +100% 净值错误被白名单吸收 | 物理量界 + 精确恒等式（V5 复核） | ✅ 完成 |
| R4A-P1-1 | `target_weight` schema 默认值与实现相反 | `bench-60-40` 静默变满仓 | 删除矛盾默认值 + 机制钉（66/66 变异全抓） | ✅ 完成 |
| R5-P1-1 | 生产 admin 仍用源码可见默认口令 + 明文 HTTP + cookie 无 Secure | 账号等于公开 | 仅**可见化**（启动响亮告警）——改行为属运维决策 | ⏳ **待你决定（最高优先）** |
| R6-P1-1 | 退化腿置 None 后两处 Δ 直接相减 | 零成交场景反而 `failed`（我 R5 引入的回归） | `_delta_or_none` 统一语义 + `skipped` 计数 | ✅ 完成 |
| R11B-F1 | ETF 最小变动单位 0.001 却按 0.01 舍入限价 | **39 天假涨停 + 28 天假跌停**（真实 run 已误拒 5 笔买入；另有 8 天真跌停被漏判） | `_tick_for(asset_type)`：ETF 0.001 / 股票 0.01 + 数据实证钉子 | ✅ 完成 |
| R11A-F1 | 旧引擎批量落库路径零退化闸门 | 噪声（17022）被当"基准夏普/超额夏普"落库并经前端/CSV/API 外显 | `extract_cell` 过闸 + `excess_sharpe` 任一侧不可用即 None | ✅ 完成 |
| R11A-F2 | "逐年度"块零闸门，**生产库已落库** | 147 条年度块 `\|benchmark_sharpe\|>50`（max 16332.48）经 HTTP/前端/CSV 外显 | 写入面 + **读取面**双侧清噪（读取面管历史行） | ✅ 代码完成；**库内历史数据处置待你决定** |

## 四、其余材质性修复（P2，按主题）

- **研究纪律**：晋升血缘（同 config_hash 跨实验复用必须 fail-loud）、重复检测不可被"显式写出平台缺省值"绕过、退役线不得作新实验 base、关题课题的复现通路（R3）。
- **平台/通道**：MCP 不再直写 SQL、CSRF 覆盖所有变更 POST、台账 append-only 触发器迁移到存量库、已落定 verdict 库层不可改写（R1/R3/R6）。
- **数据一致性**：`live_bars` 不得覆盖真实 bar、上市日未知时如实标注（`listing_known`）、因子/qfq 的脏数据与极端值守卫（R2/R4/R11）。
- **报告与统计**：`build_report` 从 NAV 重派生也要过退化闸门、**同一缺陷类共 8 次复发**（滚动 Sharpe 逐窗口、PBO 并列半步、capture/IR/TE、课题 FDR 跳过退化腿、旧引擎链与年度块），最终收敛为"两腿结构性入口 `is_degenerate_summary` + 全消费面接线 + 旧栈幅值闸门"。
- **工程安全**：CI 不再硬编码 Windows venv、`engine_runs` 禁删守卫、parity 尾滑点白名单方向敏感。

## 五、待你决策（按优先级，含建议）

### A. 运维/安全（建议立刻处理）
1. **R5-D-1 生产口令与传输**：admin 仍可能是默认口令 `20160702`、服务为明文 HTTP、cookie 无 `Secure`。
   **建议**：先改密（`TREND_QUANT_BOOTSTRAP_ADMIN_PASSWORD` 或页面改密），再上反向代理 TLS 并把 cookie 置 Secure；
   代码侧已做启动告警，未擅自改行为。
2. **R5-D-6 生产服务自 R3 起未重启**：本轮之后仍有 **R5~R11 的应用侧修复未在在线进程生效**（含 ETF tick P1、
   旧栈闸门、CSV/HTTP 出口清噪）。
   **建议**：安排一次重启窗口（重启会自动装 12 个 `trg_engine_*` 守卫，属幂等迁移）。
3. **R11-D-3 生产库历史噪声**：`batch_backtest_cells` 147 条年度块 + 42 条 `|benchmark_calmar|>50`。
   **已止血**：读取面清噪（前端/CSV/API 不再显示 16332，已用真实行验证 194.96 → None）。
   **建议**：再做一次性 backfill（按同规则置 null + 落 `job_runs` 留痕），并花 10 分钟抽查那 42 条 calmar——
   若确认是"零回撤年除法噪声"，把 calmar 也纳入年度块闸门（属口径变更，会改现有截图数字，需你点头）。

### B. 口径/语义（建议一次性定稿并写入设计文档）
4. **R9/R10/R11 退化闸门标定**：当前幅值闸门 50 + 相对方差 1e-6；真实数据最大年化 |Sharpe| 4.37（20 日窗口 19.07），
   但 5 日窗口的**合法上限实测 49.98**（对闸门只有 1.00× 余量）。
   **建议**：改为**窗口长度感知**（闸门随 `1/√n` 缩放）或规定 `n<20 交易日不发布比值型指标`；同时把
   "退化腿"的正式定义与"记 None 的语义"写入设计文档（当前分散在实现注释里）。
5. **R11-D-1 ETF 板块幅度数据源**：现靠名称关键词（本轮用数据证明现池 20/20 正确）。
   **建议**：`instrument_metadata` 增 `limit_pct`（或 `board`）字段，由数据侧给出，代码只读；迁移期保留名称兜底。
6. **R5-D-4 共享 `compute_summary` 口径**：Sortino 下行子集、`total_return` 漏首日、PF 哨兵值——改它会重述全部存量数字。
   **建议**：冻结并文档化（"旧栈语义"），研究栈继续用新口径。
7. **R2A-P3-9 `live_bars` × 真停牌**：合成 bar 可能把真停牌标成可交易。
   **建议**：合并条件加"成交量 > 0"（需 TickFlow 停牌返回非空报价这一点被确认——请数据侧给一句结论）。
8. **R4-D-2 上市日数据源**：`instrument_metadata.start_date` 全表 NULL，"新股无涨跌幅限制"在生产库整条不生效。
   **建议**：明确该字段语义（上市日 vs 回填起点）并补数据（tushare 有 `list_date`）。
9. **R1-D-5/D-6/D-7**：课题 FDR 的 p 值族混用、bucket 池化 vs 横截面分位、创建型实验缺少高原证据——
   三条都是"口径选择"，建议在下一份架构修订里一次性写明期望口径。
10. **R11-D-2 `_PRICE_TICK_EPS`**：5e-4 的经验容差（真实 run 有利价差 0/191，当前无风险）；若引擎改成交价参考口径需复核该常数。

### C. 流程/文档
11. **R4-D-3 验收脚本与开发日志数字**：slow 验收钉只断言结构不重算；开发日志阶段 2 快照是修前数字。
    **建议**：把验收钉升级为"重算一条线并与最新 run 对差"，日志加一段"数字已因除权基准修复重跑"的注记。
12. **R4-D-4 分层例外**：`core/jobs.py → gateway/portfolio`、`module_gate L4→L2`、`rule_backtest.metrics` 复用、
    `data→services` —— **建议**：作为"已接受的例外"写进架构文档，避免下一轮审查反复讨论。
13. **R3-D-1 已关课题的复现通路**、**R5-D-5 `research_runs.role` 列**、**R1-D-8 `phase_combo_entry@1`/`composite@1`
    文档-注册表落差** —— 建议排进下一期开发项（都是小改动，但需要迁移/文档同步）。
14. **R1-D-1 python module gate 信任模型**：加载的模块在进程内执行；已做属性黑名单等低成本收紧，残余风险需
    "门与装载迁到子进程/容器"才能根治。**建议**：在开放 AI 自建模块之前完成该迁移。

## 六、验证方法（为什么这些数字可信）

- **独立复算**：每轮由不同代理从零重实现并逐日对差——复权（167 万行 × 49 标的，0 不一致）、可交易性限价
  （874 标的 × 662 日 = 111 万次比较，0 不一致）、组合报告/统计件/PBO/DSR/PSR（数百组随机化，0 不匹配）；
- **真实数据 A/B**：改动前后在**生产库只读**上跑同一输入，量化差异（如 ETF tick：39/28/8 天；板块幅度 20/20）；
- **变异实证**：把实现改坏，确认钉子变红（累计 100+ 变异；本轮 19 个全部命中）；
- **跨进程确定性**：NAV/成交/判定/物化产物在多次进程与 `PYTHONHASHSEED` 下位级一致；
- **生产库只读核验**：`integrity_check=ok`、12 个 `trg_engine_*` 守卫在位且定义与代码 DDL 一致、迁移幂等无数据丢失。

## 七、Round 12 结果与终止条件

**Round 12：NOT CLEAN（6 P2，全部已在本轮修复）**，详见 `round12-review.md` / `round12-fixes.md`：

- **R12A**（对 Round 11 修复做对抗证伪）：6 组修复**全部证伪失败**（即修复成立：ETF tick 269/269、
  板块幅度 20/20、engine_runs 守卫与迁移、parity 方向 0/39 假阳性、旧栈闸门六面无逃逸、
  13 条钉子 14/14 变异全红）；但抓到 **退化腿类第 9 次复发**——闸门漏了"单策略跑批"整条消费面
  （噪声 3680 直达前端），以及**因子守卫的边界仍然漏**（f=1e3 → limit_up=0.001 且判涨停）。
  修复：闸门**移到引擎 summary 出口**（单点收口，四条消费面一次覆盖）+ 因子改"两层互证"判据。
- **R12B**（存量面新审）：4 项 P2——**批量导出跨用户泄露实盘成交**（已部署 HTTP 面上，
  用非 admin 用户实测可拿到他人 15 笔）、实盘清单的**老持仓止损被静默取消**（400 天窗口）、
  **T+1 可卖量按最早入场日虚增**（清单会下发不可卖的卖出）、**旧栈批量缺运行期冻结门**
  （批次 37~44 分钟跨 16:30 会读到两版价格）。全部已修 + 6 条新钉子（8 个变异全红）。
  R12B 的独立复算也全部为反证：26 笔真实持仓 × 11 字段 0 差异、全池 875 标的 qfq/指标重建 0 差异、
  调度单飞正确、研究纪律无缺口。

**终止条件状态**：已 18 轮（≥3 ✓）；**连续干净轮仍未达成**——R15 又抓到 **1 个 P1**
（创业板涨跌停幅度无日期维度：2015-01~2020-08-21 真实 ±10% 被判成 ±20%，生产库 13 笔不可达
成交、1437 个 symbol-day 受影响，并污染了已物化的 base-v1 结论）。该 P1 能躲过前 14 轮的原因是
**既有断言把错误口径写死了**（与 R1 的除权基准同型）。这说明"多轮干净"仍不是收敛的充分证据，
本循环继续。
R13 的性质与前 12 轮不同：**没有新机制缺陷**，3 条 P2 全部是"我上一轮声称已修、实际未闭合"
（影子现金只改了一半、CLI 冻结门只覆盖进程内、backfill 脚本的修复从未落地）。
按材质性口径统计，最近两轮的材质性发现已从"新机制缺陷"退化为"**同一缺陷类的新消费面**"：
R11 = ETF tick（新机制）+ 旧栈闸门（同类第 8 次）；R12 = 单跑面（同类第 9 次）+ 导出权限（新面）
+ 实盘清单三处（新面）。**建议**：把"干净轮"定义为"无新增 P1/P2 材质性缺陷且无新的机制面"，
并按第六节的判据（不把 P3/钉子强度计入阻断）继续确认轮；若下一轮（R13）仍为零新增 P1/P2，
即可判定收敛（届时我会在 R13 报告中给出终止结论）。

## 七之七、R18 新增待决策点

- **R18-D-1（DSR 阈值＝研究纪律的实质选择）**：现阈值 0.0 使 DSR 门恒真 → 平台**不做多重检验折扣**
  （论文口径 ≥0.95；按现量级多数实验会停在 inconclusive）。三条路：① 采纳论文口径 + 用邻域/探针 Δ
  提供跨试验方差；② 保持 0.0 但在报告里显式写明"未做折扣"（本轮已落证据标记）；③ 折中：把
  `attempt_index` 折进 t 门（Bonferroni/Holm 式），更易向使用者解释。**建议**：先 ②（已实现）+
  在下一个课题试 ③，若你要严格对齐论文再切 ①。
- **R18-D-2（E0002 卡 running）**：审计期间代理留下的未收口实验；平台**启动收割**会在下次服务启动
  时标为 failed（与日更漏跑同一次重启即可解决）。
- **重基线批次（三次修复累积）**：R15（创业板幅度）× R17（旧栈科创板策略腿）× R18（旧栈科创板基准腿）
  三处修复都会改变旧栈历史数字 → 建议一次性重跑批量回测 + 更新物化产物 + 给产物加"口径版本"标注。

## 七之六、R17 新增待决策点（含一条运维告警）

- **⚠ 日更已漏跑（建议立刻处理）**：库内最后一根 K 线为 **2026-09-23**，而 **2026-09-24 是交易日**、
  `daily_update` 最后一次 `run_date=2026-09-23`。所有出口都如实标注 09-23 截止（无标签失真），
  但**数据已落后 2 个交易日**。根因与 R5-D-6 同一条：**生产服务不在线 → 16:30 定时任务不会触发**。
  **建议**：重启服务（会自动补跑 + 装守卫）并确认 09-24/09-25 的数据落库。
- **R17-D-1（旧栈的规则保真度：补齐还是冻结）**：旧栈不仅缺"分品种最小申报"（R17 已补），
  还**完全不建模涨跌停/停牌/T+1**。两条路：① 继续补齐（旧栈数字会继续变化、需连续重基线）；
  ② 冻结并在 API/导出/manifest 上显式标注"不建模涨跌停/停牌/T+1，仅用于相对比较"。
  **建议**：先做 ②（零风险、立刻消除误读），当旧栈成为决策依据时再做 ①。
- **R17-D-2（旧栈 336 笔的重基线）**：本轮修复会使 5 只科创板标的（688802/688795/688498/
  688809/688111）的旧格子在**重跑后**变化，与 R15-D-1（创业板幅度）同属重基线范畴。

## 七之五、R16 新增待决策点

- **R16-D-1（2023-08-10「100+1」递增）**：非科创板买入现按 100 股整数倍向下取整 →
  系统性少买 ≤99 股（≈≤0.1% 仓位）。改它会让 2023-08-10 之后全部买入股数与 NAV 变化——
  **建议与 R15-D-1（创业板幅度修复后的重基线）一并做**，避免两次重述数字。
- **R16-D-2（科创板 ETF 申报单位）**：本轮按"股票 688xxx = 200 股"判定；科创板 ETF（588xxx）
  仍按 100 份（当前池内无 588 成交）。若将来交易该品种需再核对其申报单位。

## 七之四、R14 新增待决策点

- **R14-D-1（补齐/新增标的类端点的权限）**：这些端点当前只要求登录（任何用户可触发全库 874 只
  的批量补齐）；修复只加了"冻结中拒绝"，**没加** admin 门。是否改成 admin-only 需你定。
- **R14-D-2（实盘清单的共享/隔离口径）**：已按"逐用户隔离 + admin 可全量"实现（`user_id` 列 +
  迁移）。若你的实际用法是"全队共享一份实盘计划"，说一声我改回共享并在页面显式标注。

## 七之三、R13 新增待决策点与流程改进

- **R13-D-1（短窗口的 calmar / 年化）**：60 根 +2%/日、仅一天 −0.05% 的买持腿 calmar 实测
  **267,830**、70 根一字板 `annual_return` 实测 **2.7e10**，会直达前端"卡玛比/年化"列。
  已证伪"这是浮点噪声"（由真实收益与真实回撤算出），故不属现有闸门口径；**建议**给短窗口的
  calmar/年化加**窗口长度**门槛（如 `n<20` 不发布），与 R9/R10 的闸门标定一并定。
- **R13-D-2（h2h 过拦截）**：任一腿退化时两腿 Sharpe 都被置 None，健康腿的合法值（0.9~1.2）
  在持久化报告里被隐藏——是"宁可少给"还是"各腿各判"，需要你定口径。
- **R13-D-3（profit_factor 的 999 哨兵）**：与导出 manifest 的"NULL=数据不足或不适用"冲突，
  建议统一为 NULL（会改存量导出文件的呈现）。
- **流程改进（本轮暴露）**：① 编辑失败后必须复核目标文件——R11A-F3 对 backfill 脚本的编辑
  当时失败，我没复核却在报告里写成"已修"，直到 R13B 才发现；② 修一个口径时先列出该口径的
  **全部消费者与兄弟字段**——T+1 漏了影子现金、年度块漏了同行的格子列，都是"同源只改一半"。

## 七之二、R12 新增待决策点

- **R12-D-1（实盘清单链部署前检查）**：F2/F3 修好后，建议**首次部署前**先跑一次
  `reconcile_daily_list` 对账，人工核对清单与真实持仓/可卖量一致；另需定：实盘止损基准
  用未复权实际成交价（现口径）还是复权价（会改止损价与浮盈口径）。
- **R12-D-2（导出权限模型）**：现在默认只导出调用者自己的实盘成交，admin 需显式 `live_all=true`
  才取全量。若你希望 admin 默认全量（或导出完全禁止非 admin），说一声我改默认。
- **R12-D-3（批次与日更的时间窗）**：批次现在会在运行期冻结写任务（决策 A3），
  因此 16:30 触发时若批次在跑，日更会顺延到批次结束（有 `daily_update_deferred` 留痕）。
  如果你希望反过来（批次避开 16:30 窗口启动），我可以加一个"16:00~17:00 拒启批次"的门。

## 八、遗留的已知问题（不阻断，但应知）

- 全量套件长期有 1~2 个 Windows 临时文件 flake（`tests/test_instruments_bulk_backfill.py::InstrumentAddJobManagerTest`），
  在本轮所有改动之前的基线上同样失败；
- `tests/unit/test_db_path_anchoring.py` 会在每次跑全量套件时**打开并迁移生产库**（既有行为，也是历次守卫迁移落库的途径）——
  建议改为只在临时库上验证锚定逻辑；
- 早期循环（GLM53F/DS/K3/B-/C- 系）留在生产代码里的审查编号**未清**（本轮只清了我这一轮 + 上一轮的；
  同法可再清一轮，约 34 个文件有命中）。
