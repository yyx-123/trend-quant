# Round 19 审查报告（loop-review-ds4f）

> **状态：CLOSED（2026-09-26 闭合）**
> - 修复清单与验收记录：`round19-fixes.md`；
> - 全量回归（修复后）：**1702 passed / 0 failed**；
> - ruff：(file, rule) 集合与基线 78/78 一致（新增 0 条）。
>
> 日期：2026-09-26
> 审查对象：commit `4794139`（Round 18 闭合后的 HEAD）
> 审查方式：**R19A「证伪 R18 三项修复」**（判 NOT CLEAN，2 P2）与
> **R19B「组合回测策略语义与设计文档一致性专项」**（判 NOT CLEAN，3 P2）。

## 总体结论

**NOT CLEAN（5 项 P2）**。两条是我 R18 修复的问题（其中一条是我引入的**新回归**），
三条是 L3 组合层"**文档承诺 vs 实现行为**"的差异——这一层自 Round 1 后没被系统复审过。

### R19A 的两条（针对我的 R18 修复）

| # | 问题 | 事实 | 处置 |
|---|---|---|---|
| **R19A-F1**（P2） | 我在 R18 对离线回填脚本的"透传"改动**把脚本改坏了** | `cell.get("symbol")` 对 `sqlite3.Row` 不存在（AttributeError），且 SELECT 里没有 `asset_type` 列 → 一遇待回填格子即崩（**修复前可正常运行**）；生产库当前 0 格命中故未炸 | 已修：改用 `cell["symbol"]` 下标取值 + SELECT 补 `asset_type`，并加**端到端**钉子（注入临时库真跑一次 `backfill()`） |
| **R19A-F2**（P2） | 我 R18 的 plateau 闸门"一刀切 unknown"造成**假安全** | 探针生成器对单数值参数最多给 2 个邻域点 → `verdict` 恒 `unknown` → `is_plateau`（unknown 视作通过）→ "非孤峰"条件对单参数实验**永不阻断**；实测同一组 Δ/t 在旧 2 点 1σ 判据下是 inconclusive、在新判据下是 **confirmed** | 已收敛：**保留阻断力 + 标注低置信**（见下） |

**plateau 判据的最终口径**（三轮收敛：R18B 度量 → R19A 反驳 → 本轮定稿）：
- 邻域去重后 **1 点** → `unknown`（σ 无从估计，不再静默判"高原"）；
- **2~4 点** → 仍按设计口径（Alvarez 1σ）判定（保留阻断力），但**必须标注 `low_confidence=True`**，
  告警文案带上邻域点数与"结论按低置信处理"（`plateau_peak(...；注意：邻域仅 N 个点...)` /
  `plateau_low_confidence(...)`）；
- **≥5 点** → 设计口径、无低置信标注。
- 为何不用 t 预测区间替代：2 点时该比值统计量**重尾**（H0 误判率实测 **77.9%**，因为 σ̂ 可趋零），
  换汤不换药 → 已用 MC 证伪并记录。
- "把邻域扩到 ±10/20/30% 使 ≥3 点可达"是需要口径与成本决策的选项 → **R19-D-1**。

### R19B 的三条（L3 组合层的文档-实现差异）

| # | 问题 | 文档出处 | 实测 | 处置 |
|---|---|---|---|---|
| **R19B-F1**（P2）**`heat_cap` 不是组合热上限** | 总体方案 §4.4「heat ≤ 权益×6%」、§5.13 步骤 4 | 12 只真实标的 · cap 6% · 一年：**264/365 天（72%）日结 heat 超线**、峰值 **4.15×cap**，且这 264 天**没有一天新开仓**（纯持仓浮盈漂移）；`gate_log`/`warnings`/报告**零留痕** | 已修：新增**事后口径告警** `heat_cap_exceeded(事后口径): N/M 个日结 heat 超 cap=…，峰值 …×cap——cap 仅在准入时刻卡控…不要把该 run 的敞口读作受 cap 约束`（`heat_cap_ex_post_warning()` 纯函数 + 钉子）。**硬上限需要部分卖出机制**（MVP 无）→ 文档措辞的裁决进 **R19-D-2** |
| **R19B-F2**（P2）**`all_of` 的离场语义是"任一成立"** | §5.2.8「all_of：全部子模块同时成立才生效——用于入场条件叠加…或收盘确认型复合离场」 | 600584.SS：`all_of[ma_cross20(exit), ma_cross60(exit)]` 实际卖出 **9 次**，其中 **7 次只有单腿下穿确认**（数据侧同日双穿仅 2 天）→ 与文档语义（≤2 次）差 4.5 倍 | 已按文档修：离场改为 **AND**（`set.intersection`）+ docstring 同步；生产无使用该组合的实验，故无数字翻案 |
| **R19B-F3**（P2）**`buffered_rotation` 不受 `action_gate` 门控** | §5.11 缺口①「频率参数决定哪些日子允许行动（新开仓/**轮换**/再平衡）」 | `action_gate: monthly` + `buffered_rotation`：卖出日 **27**，其中 **26 天非月首交易日**（trades 56 vs 对照 34）→ "月度"实验臂实际是日度轮换卖出 | 已修：`rotation_policy` 首行加 `allows_action` 检查（与 `rebalance_band` 同口径） |

## R19B 的其余核验（正面结论，真实 run + 文档对照）

- **可复现性**：同配置重复运行 trades/unfilled/gate_log/nav **位级一致**（`rank=random` 亦一致）。
- **参数连续性**：±1 档无跳变（risk_budget 0.0074/75/76 → 58/58/58 笔；`max_heat_pct` 0.05/0.06/0.07
  → 48/58/62 笔；`max_positions` 5→6 → 58→60 笔），无零成交悬崖。
- **真实边界**：真实跌停日 `unfilled(limit_down)` → 次日跳空穿透按开盘成交（000002.SZ 2024-10-09/10）；
  真实停牌（600584.SS 2024-03-20~26 无 bar）期间不产假意图、复牌按重算止损成交。
- **门控与限额**：`slot_limit` 最大同时持仓 5/5；三 gate 按配置序生效；`market_gate` halt 实测
  拦截 388 次/164 天（trades 28 vs 基线 42）；入场的 `action_gate` 只落月首交易日（含长假后首个交易日）。
- **权重与现金**：60/40 两腿 = equity×0.6/×0.4（含 1% 空仓计息），整手对齐，无静默超支。
- **元模块**：`any_of` 双状态并存、谁先到谁触发；`any_of` 入场集 ⊇ `all_of`；嵌套元模块被拒。

## R19B 的 backlog（不阻断，8 条）

文档旗舰示例 `trend-momentum-full` 载入即拒（`liquidity_filter` 无 `exclude_st`、`tail_session`
无 `rotation`）；`position_risk/all_of` 未注册且报错归槽误导；heat_cap×无止损价 的**告警文案与实测相反**；
缩放型 gate（`market_gate`/`drawdown_throttle` 的 `halve_risk`）不落 `gate_log`（可审计性缺口）；
`target_weight` 无 Σw 校验（超配被现金裁量静默截断）；回测路径 `pending_exits` 恒空（数字等价、
与 §5.2.5 叙述不一致）；§4.2 阻塞原因③（停牌 → `unfilled(suspended)`）回测基本不可达；
`live` 对账用 `qty`（可卖量）比 `shares`（整仓）→ T+1 残量场景假 `qty_mismatch`。

## 待决策新增

- **R19-D-1（plateau 邻域宽度）**：现为 ±20%（1~2 点）。扩到 ±10/20/30% 可得 4~6 点，让 σ 估计
  稳定、低置信标注消失；代价是探针 run 数翻倍（每个探针 = 一次完整回测）。
- **R19-D-2（heat_cap 的语义裁决）**：文档写"组合热上限"，实现只在准入时卡控。要么**改文档**
  （cap = 准入约束 + 事后告警，本轮已实现告警），要么**加机制**（部分卖出/风险回压，属新功能）。
  建议先改文档口径。
- **R19-D-3（backlog 的 8 条）**：其中"告警文案与实测相反"（B3）与"缩放门无留痕"（B4）建议尽快修
  （都是如实性问题）；其余 6 条可随下次口径统一处理。
