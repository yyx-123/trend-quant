# Round 25 修复与回归（loop-review-ds4f）

> **状态：OPEN（R25A 的 P1 未完全闭合）** —— 日期：2026-09-26
> 审查报告见 `round25-review.md`（R25B 数据层/引擎面）+ 本文件下半（R25A 证伪结果）

## 1. R25B（数据层/引擎面）发现与修复

| 编号 | 级别 | 处理 |
|---|---|---|
| R25B-F3 无涨跌幅限制日被误判涨/跌停（21 symbol-day；**已致 6 笔生产订单被拒**） | P2 | ✅ **已修**：`compute_tradability` 加"无限制日自愈识别"（无因子修正且涨跌幅超板带 2×tick 容差 → `no_limit=True`、不报涨跌停）。真实库 4 个样本日实测 `up=False down=False no_limit=True`，合法涨跌停（404 例）不受影响 |
| R25B-F7 `rematerialize_qfq` 守卫只看首末日期（中间缺口可被放行 → 整表重写成带洞） | P3 | ✅ **已修**：补"raw 日期集必须覆盖存量 qfq 日期集"判据 + 缺口天数与样例进告警（全库当前 875 只日期集一致，属防御性收紧） |
| R25B-F1 vendor 周/月 K 的 qfq 除权后整段失配（18,086 行 / 50 只，最大 −49.98%） | P2 | ⏸ **待决策**（改为本地物化 vs vendor 全历史复检；两者都会改写已落库的周/月 qfq 表）→ 最终报告第 14 项 |
| R25B-F2 "进行中的周/月 bar"以抓取日为标签落库、缺定型行（1,746 / 1,010 行） | P2 | ⏸ **待决策**（`provisional` 列 + 读侧过滤 vs 写前删旧快照）→ 最终报告第 15 项 |
| R25B-F4 `trend_rolling_daily` 无陈旧判定（改 qfq 后周/月相位可能静默用旧值） | P3 | ⏸ 记录（修法：加 `data_version` 列或读侧比对版本） |
| R25B-F5 成交价未按 tick 取整（98% 的成交非 tick 整数倍，最大偏差 0.39%） | P3 | ⏸ **待决策**（会改动全部历史回测数字，建议与"重定基线批量"合并）→ 第 16 项 |
| R25B-F6 费用模型缺过户费（股票 0.001% 双向 ≈ +3.05% 手续费） | P3 | ⏸ **待决策**（同上，建议合并）→ 第 16 项 |

**R25B 已测不成立**：复权恒等式（875 只 × 1,673,762 行零误差）、因子落点约定（6,103/6,105）、
qfq 断裂扫描无复权断裂、vendor 周/**raw** = 本地日 K 聚合（六列零误差）、日线缓存 `data_version`
机制有效、引擎边界矩阵 23 例全对、真实 30 run / 40,892 行 NAV 恒等式 0 行不符。

## 2. R25A（证伪 R24）结果与修复

**R25A 结论：R24 的 10 组修复被部分证伪**——tdist / plateau 校准 / bucket 代价 / 迁移并发
（日常路径）/ token 五条成立；但 1 条 P1 未闭合 + 11 条新发现（含 3 条是我在 R24 引入的回归）。

| 编号 | 级别 | 现象 | 处理 |
|---|---|---|---|
| **R25A-F1** | **P1** | R24 的簇重抽只按**单日**抽，覆盖不了"同/邻日事件共享同一段未来收益"（主 horizon 10 日、80% 窗口重叠、日级 ACF 0.42）——真实结构下 H0 拒绝率 **29.5%~50%**（1 个事件日 86%、2 个 53%、5 个 16.5%） | ⚠️ **部分修复**：改为抽**连续 N 日区块**（块长 = 主 horizon）+ 加"事件日 <10 不出判定"的门。我的 MC 复校（重叠窗口 + burst 聚簇）：41.2% → **17.5%**（仍高于名义 5%——我的复算 DGP 以 0 为零假设，而真实代码以同窗基线均值为基准，两者不可直接比；**该 P1 尚未完全闭合**，下一步建议 stationary bootstrap 或按自相关长度标定块长 + 用同窗基线做零假设的 MC） |
| R25A-F2 | P2 | `_round_trips_with_net` 用 `(symbol, exit_date)` 当唯一键 → 同日双平仓互相覆盖（1489.0 → 489.0）——R23B-F9 已在成交侧修过的同一形态被我在 R24 重新引入 | ✅ **已修**：改为逐笔顺序消费 + 落 `unmatched_round_trips` 计数 |
| R25A-F3 | P2 | `pnl_basis="net"` 无条件写入，匹配不到净额的条目仍带毛额 → 声明与内容矛盾 | ✅ **已修**：按**实际产出条目**推导（全部有 `pnl_net` 才 "net"，否则 "mixed"） |
| R25A-F4 | P2 | `pair_round_trips` 的费用仍只认 `fee_total` → 只有 commission/stamp_tax 的成交净额 = 毛额（同一笔差 21.0） | ✅ **已修**：提模块级 `_fill_fee`，回合净额与费用汇总共用 |
| R25A-F5 | P2 | bucket **内部**空桶不阻断判定（实测判出 `confirmed` 而告警写着"按 inconclusive"） | ✅ **已修**：`all(np.isfinite(m) for m in means)` 才计算 spread/单调性 |
| R25A-F6 | P2 | `gate_alpha=0.05` 是误导口径（联合门等效 α 实测 0.25%~0.33%） | ⏸ 记录（建议：把"p 腿 + 方向腿"的等效 α 落库并写进文档，或把方向腿改成软条件） |
| R25A-F7 | P2 | `_migrate_schema` 的"单事务"被 `executescript` 的**隐式 COMMIT** 打断 → 半迁移态可落盘（`portfolio_live_lists_old` 残留 + 新表 0 行）；R24B-F2 声称消灭的状态再现 | ⏸ **待修**（拆分 `_LIVE_LISTS_DDL` 为逐条 `conn.execute` 或整段放进同一 script 的 `BEGIN…COMMIT`；并加 `_old` 残留自愈）。仅对"旧 schema 库"触发，现网库已迁移 |
| R25A-F8 | P3 | PBO 交集为空时静默 `None`（无告警） | ✅ **已修**：落 `pbo_unavailable(no_common_dates)` |
| R25A-F9 | P3 | equity≤0 → 收益 ±inf → PBO 报 `0.0` 且 `degenerate_variants=False` | ✅ **已修**：非有限收益置 NaN |
| R25A-F10 | P3 | 逐笔区块 bootstrap 在 n ≤ block 时退化为**零宽带**（R24 引入的回归） | ✅ **已修**：块长上限 `min(block, n//3)` |
| R25A-F11 | P3 | 统计基元 fail-open：`t_sf(NaN)` → 0.0（最强证据）、`paired_gate_ok` 在 `n_pairs` 缺失/为 1 时用最松临界 | ✅ **已修**：NaN → 1.0；`n_pairs < 2` 不出门 |
| R25A-F12 | P3 | bucket 落库的 `n_permutations` 仍是 200（实际 2000） | ✅ **已修**：提常量并共用 |
| R25A-F13 | P3 | 事件数不足时 bucket 全静默（读者无法区分"样本不足"与"算不出来"） | ✅ **已修**：落 `insufficient_events(n < m)` 告警 |
| R25A-F14 | P3 | 血缘：不存在的 id 返回"幽灵链"；>32 级复现链的祖先 token 静默失效 | ⏸ 记录（修法：入链前确认行存在；超深度显式告警） |

## 3. 本轮回归

- R24 钉子 15 条全绿（含新增的无限制日钉子）；R23 钉子、配对/统计单测、研究栈集成全绿；
- 全量 `.venv` 套件：见提交说明；
- ruff：与本轮前基线相比**无新增**。

## 4. 结论（必须如实说明）

- **R25A 的 P1（event 簇结构）未完全闭合**：修法方向已落地（时间块 + 最小簇数门），
  但我的复校显示残余超名义水平，需要在下一轮用"同窗基线零假设 + 自相关标定块长"的 MC
  重新标定，**在此之前不得声称事件研究的显著性口径已正确**；
- R25A-F7（迁移原子性）与 F14（血缘边界）仍是待修项；
- R25B 的两条 P2（周/月 qfq 失配、进行中 bar 语义）与两条会改数字的 P3（成交价取整、过户费）
  需要你先给口径方向（最终报告第 14–16 项）。
- 因此**本轮不闭合**：R25 之后仍需要至少 R26 来完成"连续 2 轮无新增 P1/P2"的终止条件。
