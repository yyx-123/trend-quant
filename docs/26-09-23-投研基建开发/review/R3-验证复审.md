# R3 验证复审（三审修复轮的独立验证）

> 复审人：R3 独立验证代理（对 K3-复审-R2 / Deepseek复审报告 / GLM53F盲审报告 的修复轮做终审验证）
> 日期：2026-09-24
> 复审对象：第九轮修复（见 `2026-09-23-开发日志.md` 末尾）——2 项放行条件 + 5 项 P1 + P2 批
> 方法：逐条代码走读 + 自建探针独立实证（`python -c`/临时脚本 + tempfile 临时库，跑完即删）+
> 新钉子两文件单独复跑 + 全量回归复跑（我的命令，不复用开发侧数字）
> **只读声明**：未改动任何代码/文档/数据库；唯一写入 = 本报告文件。
> 环境口径：全部命令用 `.venv/Scripts/python.exe`（Python 3.12.10）。

---

## 0. 总体结论

**判定：PASS——两项放行条件与五项 P1 全部实证关闭，P2 批抽查 16 项全部落地且配真钉，
钉子有效性抽查 15+ 条无假钉/重言式，全量回归 1496 通过 / 0 失败，唯一单跑失败为
改动前即存在的已知 Windows flake。未发现假修复；新发现 4 条非阻断注记（§K）。**

| 复审项 | 结论 | 关键证据 |
|---|---|---|
| A. DS-R2 新 P1 启动收割 | **PASS** | 探针三场景实证（§A） |
| B. K3-R2 残留-1 bucket 去重 | **PASS** | 与 event.py 逐行同构 + 钉子可失败推理（§B） |
| C. GLM53F-P1-1 ETF 涨跌停幅度 | **PASS** | 探针实证 ±20%/±10%/db=None 三形态（§C） |
| D. GLM53F-P1-2 元模块转发+成员校验 | **PASS** | 探针实证转发与 ValueError（§D，附 1 条 P4 注记） |
| E. GLM53F-P1-3 live 止损重建 T-1 | **PASS** | 探针 6 场景独立重算对拍（§E，附 1 条 P3 注记） |
| F. GLM53F-P1-4 verdict 反劫持 | **PASS** | 4 展示面全走 canonical + 探针边界（§F，附 1 条 P4 注记） |
| G. GLM53F-P1-5 concentration_cap 钉 | **PASS** | 双处真断言 + 真实调用形态（§G） |
| H. P2 批抽查（16 项） | **PASS** | 逐项见 §H |
| I. 钉子有效性抽查（15+ 条） | **PASS** | 全部"删实现即失败"（§I） |
| J. 回归 | **PASS** | 1496 passed / 0 failed（481s）；flake 单跑确认（§J） |

---

## A. DS-R2 新 P1：启动收割谓词收窄 —— PASS

**代码**（`src/research/lifecycle.py:157-186`）：

```sql
WHERE status = 'running'
   OR (status = 'evaluating' AND id NOT IN (
         SELECT experiment_id FROM research_verdicts))
```

谓词与建议修法逐字一致：running 全收；evaluating 仅在无平台 verdict 时收；
engine_runs 只收 running（不再含 evaluating——evaluating 实验的 run 应已完成）。
`research_verdicts.experiment_id TEXT NOT NULL`（`db.py:320`），`NOT IN` 无 NULL 中毒风险。

**我的探针**（tempfile 临时库，三场景 + 二次收割）：

```
sweep: {'experiments': 2, 'engine_runs': 0}
evaluating+verdict   -> evaluating   （保留）
evaluating+无verdict -> failed       （收割）
running              -> failed       （收割）
confirm after sweep: inconclusive | exp status: verdicted   （收割后仍可确认）
second sweep: {'experiments': 0, 'engine_runs': 0}
```

即 DS 报告的实证场景（重启 → 误判 failed → 永不可确认）已不再发生。

**钉子**：`test_startup_sweep_preserves_evaluating_with_verdict`（存活 + confirm 成功）、
`test_startup_sweep_reaps_orphans_only`（双收割 + 计数=2）——两向俱全，均在我复跑的 31 项里通过。

## B. K3-R2 残留-1：bucket_analysis 事件去重 —— PASS

**与 event.py 同构性**（逐行对拍）：

| 要素 | event.py | bucket.py |
|---|---|---|
| (symbol,事件日) 去重 | `seen_events`，key `(ev.symbol, ev_day.isoformat())`（:217,:239） | 同（:158,:174） |
| 事件日→索引 | `date_to_idx.get(ev_day)`（:234） | 同（:171） |
| 前瞻收益取事件日 | `matrix[t, c]`，t=ev_t（:257） | `matrix[t, col]`，t=ev_t（:199-201） |
| 特征值取事件日 | —（event 无特征） | `feat[ev_t, col]`（:178）——**不再取 scan 日** |
| 前瞻不足排除 | `ev_t + max_h >= len → skip`（:235） | 同（:172） |

**钉子可失败性**（`test_bucket_dedupes_event_within_valid_days`，读码推理）：
探针信号 `_RepeatEventSignal` 在 3 个 scan 日（03-01..03-03）各为 5 个标的上报
同一事件日（03-01）。去重在位 → 5 事件（首 scan 日落 key，后两日全 skip）→ 断言
`events == 5` 成立。**删掉 seen_events 检查 → 3×5=15 事件 → 断言失败**。钉子真实。
（特征值"取事件日"在本探针下与"取首个 scan 日"数值重合——dedup 恰保留事件日==scan 日
的首次报到；此差别由与 event.py 的同构代码路径承担，可接受。）

**既有锚重锚**：`test_research_evaluations.py:151-154`——monotonicity 0.5 /
q_spread −0.00907 / events 825（去重后），全部在全量回归中通过，与开发日志声明一致。

## C. GLM53F-P1-1：ETF 涨跌停幅度 —— PASS

**代码**（`src/gateway/tradability.py:41-62`）：`board_limit_pct(symbol, *, asset_type=None,
name=None)`——asset_type=="etf" 时：沪 588xxx → ±20%；名称含"创业板/科创" → ±20%；
其余 ±10%；无 asset_type → 股票前缀规则（历史口径）。compute_tradability 接线：
`asset_info` 参数 → `info.get("asset_type")/info.get("name")`（:168-170）；
db=None 且 listing_dates/asset_info 缺省 → 补空 dict 不触库（:124-128）。

**我的探针**（直接调真实函数）：

```
600519.SS: 0.10 | 300750.SZ: 0.20 | 688981.SS: 0.20          （股票规则不变）
159915.SZ etf(创业板ETF易方达): 0.20 | 588000.SS etf: 0.20 | 510300.SS etf: 0.10
510300.SS 无 asset_info: 0.10（历史口径）| 159915.SZ etf 无 name: 0.10（保守缺省）
[db=None 不传 listing/asset_info] → 正常出帧不炸（fallback ±10%）
[159915 +12%] limit_up_price=13.20 (11.0×1.20), is_limit_up=False   ← 旧代码此处误判 True
[159915 +20%] limit_up_price=13.20, is_limit_up=True
[510300 +12%] limit_up_price=12.10, is_limit_up=True               ← 主板 ETF 正确卡买
```

深市 ETF 无 name 时回落 ±10% 属保守缺省（生产 asset_info 取自 instrument_metadata，
name 正常在库），与 docstring 声明一致，非缺陷。

## D. GLM53F-P1-2 + P2-19：元模块转发 + 成员参数校验 —— PASS

**代码**（`src/portfolio/slots/execution.py`）：
- `_MetaBase.prepare_with_gateway`（:182-187）逐子转发（hasattr 守卫，普通成员不炸）；
  AnyOfSignal/AllOfSignal/AnyOfPositionRisk 三槽共用（继承 _MetaBase）——顶层
  `hasattr(signal_mod, "prepare_with_gateway")` 探测命中元模块即正确下探。
- 成员参数校验（:173-179）：逐成员 `validate_params(spec.params_schema, sub_params)`，
  失败抛 `ValueError("meta member ... params invalid: ...")`。

**我的探针**：

```
any_of(position_risk) 包 hard_stop{atr_mul:-5} → ValueError: meta member hard_stop@1
    params invalid: param atr_mul: -5.0 < min 0.1
atr_mul:1.5 成员 → 正常实例化
any_of 顶层探测 hasattr(prepare_with_gateway)=True；转发调用记录 (symbols, start) 精确命中
all_of 转发同样命中；ma_cross 普通成员（无该方法）转发不炸
```

**注记（P4，非阻断）**：顶层参数在 `parse_strategy_yaml`（载入时）即拒，元模块成员在
`instantiate_modules`（run 开始时）才拒——我的探针确认 `parse_strategy_yaml` 接受
meta 包裹的 −5、实例化时抛错。fail-loud 在任何成交前成立，钉子测的 factory 层即校验
所在层；"载入即拒绝"对元成员晚一步，属可接受口径差（建议随手在 strategy.py 载入路径
下探 members 校验，非本轮条件）。

## E. GLM53F-P1-3：live 止损重建 T-1 口径 —— PASS

**代码**（`src/portfolio/live.py:87-164`）：
- 末根 provisional 排除：`state_end = upto-1 if last_is_provisional else upto`（:105-108）
  ——post-close 重跑（末根=真实收盘）不误排；
- 吊灯 = `highest(T-1) − mul×ATR(T-1)`（:153，highest 取 `[idx:state_end+1]`，ATR 取
  `_atr_through(state_end)`）；
- 棘轮 = 持仓期逐日（running_high − mul×ATR(d)）候选累计 max（:141-151）；
- ATR period 用模块参数 `getattr(module, "atr_period", 20)`（:116，不再硬编）；
- 当日买入 `state_end = max(state_end, idx)`（:108）——状态自洽口径（回测 init_stop
  同为入场日含当根），且当日 sellable=0（:77）。

**我的探针**（6 场景，独立重算对拍，全部吻合）：

```
[1] 末根 provisional(999) 排除:  highest=15.9580 (<100), stop=15.2155 == 独立重算
[2] post-close 真实末根(high=20): highest=20.0000（含末根，不误排）, stop 对拍一致
[3] 当日买入(state_end==idx):     状态用入场日 bar，stop_price 可算（自洽）
[4] 棘轮: stop=15.2155 == 逐日累计 max 独立重算；highest<100
[5] atr_period=10: 按模块参数生效，stop 对拍一致
[6] hard_stop: 用入场日含当根 ATR（决策 6），对拍一致
```

两个库内钉（`test_live_stop_rebuild_excludes_provisional_spike` /
`test_live_stop_rebuild_ratchet_running_max`）均为独立重算值断言（非重言式）。

**注记（P3，非本轮引入）**：`evaluate_exits` 无 sellable 过滤——当日盘中买入+当日极端
冲高的组合理论上可把一笔 T+1 不可卖的持仓列入 14:00 应卖清单（`sellable=0` 已置位但
清单层不消费）。边缘场景、先前轮已存在的形态，本修复未引入亦未恶化（次日重建用真实
数据），记录在案供下轮顺手收口。

## F. GLM53F-P1-4：复核 verdict 不劫持课题摘要/展示 —— PASS

**共享选择器**（`src/research/verdict.py:99-106`）：`canonical_verdict`——supersedes
IS NULL 优先（原生/确认链），同类链内取最新，全空兜底取末位；`latest_verdict`
SQL `ORDER BY (supersedes IS NULL) DESC, id DESC`。

**展示面接线（逐一核对，无 `verdicts[-1]` 残留）**：
- `conclusion.py:59-68` 内联同语义选择器（见下探针一致性验证）；
- `api.py:116`（get_run_report）；`api.py:96/201/250`（latest_verdict）；
- `src/app/routers/research_ledger.py:78`（看板行）与 `:127`（report.json 下载）；
- `topic_files.py:65`（物化）。

**我的探针**：canonical 选择器 6 种边界形态（原生+复核→原生 / 复核在前→原生 /
双原生→取新 / 全复核→取新 / 空→None / 复核不劫持）全部符合预期；在真实调用形态
（id 升序，全部调用方经 `list_verdicts ORDER BY id`）下与 conclusion.py 内联选择器
**6/6 全排列一致**。钉子 `test_conclusion_canonical_verdict_and_hypothesis_direction`
构造（原生 confirmed×3 + 复核稿 final=rejected 带 supersedes）断言计数 confirmed=3 /
rejected=0、方向一致率 1/3、分级 insufficient-evidence——同时钉死 P2-2（与假设同向，
非多数方向），在我复跑中通过。

**注记（P4，非阻断）**：`canonical_verdict` docstring 声明输入须按 id 升序；喂乱序列表
时它与 conclusion 内联选择器可分叉（我构造出过）。真实调用面全部 ORDER BY id，
纯理论边界，建议后续在函数内加一行 `sorted(verdicts, key=id)` 防御即可。

## G. GLM53F-P1-5：concentration_cap 真断言 —— PASS

- `tests/unit/test_review_r3_unit.py:275-319`：两只 L2A 持仓 + [L2A 第三只, L2B] 双意图
  → `out == ["M03.SS"]` + `gate_log` 含 concentration_cap 条目；桩签名
  `instruments(wanted=None)` 可承真实无参调用（实现为 `ctx.gateway.metadata.instruments()`，
  `portfolio_risk.py:109`）。
- `tests/unit/test_module_behaviors.py:321-355`（弱断言补强处）：同型真断言
  （拦截面 + `g["symbol"]=="M02.SS"` 的 gate_log 留痕）。

删掉 gate 拦截逻辑 → `out` 含被拦标的 → 两钉即失败。原 `isinstance(out, list)`
重言式已被替换。两钉均在复跑中通过。

## H. P2 批抽查（16 项）—— 全 PASS

| # | 项 | 证据 |
|---|---|---|
| 1 | holdout token 收窄 | `pipeline.py:71-86` 只取 `experiment_id = ?` 的绑定 token；全局 token 须显式透传。钉两向：全局不自动带出（run failed + error 含 holdout）+ 显式透传跑通并消费；绑定 token 自动带出。均通过 |
| 2 | 配对检验按日期交集 | `backtest.py:536-550`：`pd.concat(join="inner").dropna()` + `dates_dropped` 留痕。我的探针：base 缺 3 天 → joined=97/dropped=3；错位对拍 t=0.817 vs 0.321（日期 join 确实改变结果） |
| 3 | parity 归因 | `engine/parity.py`：`max_tail_slippage=0.011` 价差界限（`0 < abs(nt/ot−1) ≤ bound` 才归 tail_slippage）+ unexplained 桶 + `nav_length_mismatch`。钉 `test_parity_tail_slippage_bound_and_unexplained` 三段断言（0.5% 归白名单 / 5% 进 unexplained / nav 长度差不静默截断）通过 |
| 4 | worker daemon 池 + 调度守卫 | `worker.py:29-54` `_DaemonThreadPoolExecutor`（我的探针：本机 3.12.10 实际拉起 daemon=True 线程且任务正常完成）+ `:102-110` 回退普通池；`:134-143` 调度循环 try/except + 退避；`:179` `_run_one` 只收仍为 queued 的孤儿（P2-10 收敛）。钉通过 |
| 5 | CLI/MCP 冻结写 | `scripts/research_cli.py:112-118` 与 `trend_mcp/research_tools.py:53` 均包 `frozen_writes()`；MCP 钉断言进入过上下文；CLI 另补 `--db`（:38） |
| 6 | 草稿装载隔离 | `modules.py:243-259` 逐条 try/except + 日志跳过；钉（坏草稿 loaded==0 不抛）通过 |
| 7 | 冻结顺延补跑哨兵 | `jobs.py:138-164`：daemon 哨兵轮询解冻（2h 上限）→ 当日重跑；跨日交还 cron。钉（deferred + sentinel 挂起并自行退出）通过 |
| 8 | 方向一致率与假设同向 | `conclusion.py:89-95` 用 `spec.expect`；钉构造效应 [−0.2,−0.1,+0.05] 全 positive 假设 → 1/3（旧口径 2/3，可区分）通过 |
| 9 | 重复检测类型逃逸 | `experiments.py` `_coerce_scalar` + `_values_similar` 归一化。我的探针 8 断言全过（"2.0"≡2.0、true≡True、2≡2.0、±10% 界、bool≠数值、"abc" 不相似） |
| 10 | regime 基准不可用禁用拆分 | `backtest.py:456-483`：bench300 无数据 → `bench_nav_for_regime=None` → `_regime_split` 返 {} + `regime_benchmark_unavailable` 警告；`base_nav` 分支仅在 base 即 bench300 时用（合法，非回退）。钉通过 |
| 11 | 长窗口三注记下发 | 共享件 `_common.py:162`，接线 backtest:636 / event:337 / bucket:253 / distribution:97 四处；三钉（event+distribution、bucket）断言三前缀齐全，通过 |
| 12 | live 对账 ref_price + qty | `live.py:414-439`：bench=ref_price（收盘基准）双侧 + qty_mismatches。钉断言 diff_pct=0.01 vs bench 10.00（非 est 10.03）+ qty 1000≠900，通过 |
| 13 | 新股豁免分板块/分时代 | `_ipo_no_limit_days`：ETF=0；科创 5 日；创业板 2020-08-24 起 5 日否则 1；主板 2023-04-10 起 5 日否则仅首日。三钉（旧规首日/注册制 5 日/ETF 无豁免）通过 |
| 14 | promote AI 闭环（用户决策反转） | `api.py promote_to_library` 无 human 门，唯一门=血缘门（verdicted + final=confirmed）；docstring 明记 2026-09-24 用户决策。钉两向：未确认实验 AI 晋升被血缘门拒（ResearchError）/ confirmed 实验 AI 会话晋升成功且带 parent 血缘，通过 |
| 15 | 小样本 t 临界 + 判定警告 | `verdict_rules.py:43-53` Cornish–Fisher（t(30)=1.697/t(10)=1.812 对表值）；gate 用 `max(min_t_stat, _t_critical_95(n−1))`；`plateau_evidence_absent`（backtest.py:645）与 `turnover_jump>50%`（:652）进 warnings。钉通过 |
| 16 | attempt_count 排除复现 | `api.py strategy_line` 过滤 `is_reproduction`；钉（rerun 后 attempt=1 / experiments=2）通过 |

## I. 钉子有效性抽查（15+ 条，删实现即失败的推理验证）—— PASS

| 钉子 | 删/回退什么会失败 |
|---|---|
| test_startup_sweep_preserves_evaluating_with_verdict | 谓词回退 `IN('running','evaluating')` → status=failed + confirm 抛 LifecycleError |
| test_startup_sweep_reaps_orphans_only | 谓词漏 evaluating 无 verdict 分支 → exp_eval 留 evaluating |
| test_bucket_dedupes_event_within_valid_days | 删 seen_events → 15≠5 |
| test_board_limit_pct_etf_rules | 删 ETF 分支 → 159915 返 0.10 ≠ 0.20 |
| test_etf_limit_up_12pct_chinext_tracker_not_blocked | 删 asset_info 接线 → ±10% 下 12.32≥12.10 → is_limit_up=True，断言 not 炸 |
| test_meta_signal_forwards_prepare_with_gateway | 删 _MetaBase.prepare_with_gateway → hasattr=False 即失败 |
| test_meta_member_params_schema_validated | 删成员校验 → -5 被接受 → pytest.raises 失败 |
| test_live_stop_rebuild_excludes_provisional_spike | 删 provisional 排除 → highest=999 → `<100` 炸 |
| test_live_stop_rebuild_ratchet_running_max | 棘轮退化端点式 → 与逐日累计重算值不合 |
| test_concentration_cap_blocks_third_same_l2 | 删拦截逻辑 → out 含 M02.SZ + 无 gate_log |
| test_parity_tail_slippage_bound_and_unexplained | 删价差界限 → 5% 价差归白名单 → unexplained==0 |
| test_t_critical_95_small_sample / test_verdict_gate_uses_t_critical_for_small_samples | 退回 z=1.645 → t(10)≈1.812 不符；n=31 时 t=1.66 被误放行 confirmed |
| test_conclusion_canonical_verdict_and_hypothesis_direction | 回退 verdicts[-1] 覆盖 → confirmed=2/rejected=1 计数炸；回退多数方向 → 2/3≠1/3 炸（双向钉） |
| test_global_token_not_auto_picked_but_explicit_pass_works | _pick 回退取全局 → run 不 failed → 断言炸 |
| test_dispatch_loop_survives_probe_exception | 删守卫 → 调度线程死 → is_alive()=False |
| test_mcp_sync_run_wraps_freeze / test_load_reviewed_modules_isolates_bad_draft / test_freeze_defer_spawns_same_day_catchup | 删对应实现 → entered 空 / exec 抛错 / sentinel=None |
| test_live_reconcile_uses_ref_price_and_checks_qty | 回退 est_price → bench=10.03 diff≈0.698%≠1%；删 qty 核对 → 空列表≠期望 |
| test_promote_allows_ai_full_loop | 重加 human 门 → AI 晋升抛错；删血缘门 → 未确认实验晋升成功 → pytest.raises 炸 |

抽查密度超过要求的 6 条，未发现重言式或"删掉也全绿"的假钉。

## J. 回归复跑 —— PASS

- **全量**：`.venv/Scripts/python.exe -m pytest tests/ -q --deselect tests/test_instruments_bulk_backfill.py`
  → **1496 passed, 5 deselected (slow), 0 failed，481.28s**。与开发日志自报（1496/0）一致。
- **新钉单独**：`tests/unit/test_review_r3_unit.py` + `tests/integration/test_review_r3.py`
  → **31 passed（15+16），30.99s**。
- **flake 单跑**：`tests/test_instruments_bulk_backfill.py` → **1 failed, 4 passed**：
  `test_rejects_second_add_job_while_running`，`PermissionError: [WinError 32]` 于
  tempfile 清理 `tmp.../test.db`（tempfile.py:909）——与三份报告记载的改动前即存在的
  Windows 临时目录清理竞态完全同型，与本轮改动零交集。

## K. 新发现（全部非阻断，不构成 FAIL 条件）

1. **P3（文档漂移）**：`tests/integration/test_review_r3.py` 文件头 docstring（:9）与
   分节注释（:232-234）仍写"promote 入库门仅 human session（AI 通道被拒）"，与该文件
   内实际钉子 `test_promote_allows_ai_full_loop`（用户决策反转后：AI 闭环放行）相反。
   纯注释陈旧，测试本体正确——建议随手改两行注释。
2. **P3（先前轮已存在）**：`evaluate_exits` 无 sellable 过滤，当日买入 + 当日极端盘中
   冲高理论上可进 14:00 应卖清单（见 §E 注记）。未由本轮引入。
3. **P4**：`canonical_verdict` 依赖输入按 id 升序（真实调用面全部满足）；乱序输入下与
   conclusion 内联选择器可分叉。加一行 sorted 防御即可根除。
4. **P4**：元模块成员参数在实例化（run 前）而非 YAML 载入时拒绝，比顶层晚一步
   （见 §D 注记）；fail-loud 语义不受影响。

---

## 结论

三份报告的 2 项放行条件与 5 项 P1 全部实证关闭；P2 批抽查 16 项落地；钉子体系经
15+ 条抽查无一假钉；全量回归 0 失败，唯一失败项为已知存量 Windows flake。promote
AI 全流程闭环按 2026-09-24 用户决策反转实现（血缘门唯一），验证为有意设计。
未发现假修复、无效钉子或本轮新引入的 P1/P2 问题；§K 四条注记均为 P3/P4 级。

R3_FINAL_VERDICT: PASS
