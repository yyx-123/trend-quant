# 意见调和记录（reconciliation）

- 日期：2026-08-24
- 作用：记录 v1 → v2 过程中，三方审查意见（独立审查 `independent-review.md`、评审 A `review-a-factcheck.md`、评审 B `review-b-coverage.md`）的逐条处理结果——采纳 / 部分采纳 / 驳回及理由。本文件即「修订去向表」，确保没有意见被无声丢弃。

---

## 1. 评审 A（事实核查）意见处理

| 编号 | 意见 | 处理 |
|---|---|---|
| F-1 | P0-2 的「frp 暴露公网（README/部署文档）」引用来源不存在 | **采纳**。v2 P0-2 改为引用 `scripts/deploy.sh`（nginx listen 80 公网可达）+ `main.py:381-383` 的 frp 自述注释，并把「两种部署形态描述矛盾」单列为 §9-6 与待裁决事项 4 |
| F-2 | 测试文件数 70 → 61 | **采纳**。v2 §0/§8 已更正（61 个 test_*.py） |
| F-3 | src 行数 1.2 万 → 16,816（约 1.7 万） | **采纳**。v2 §0 已更正 |
| F-4 | §5 第 12 条行号张冠李戴（fetch_minute_history 在 305-313） | **采纳**。v2 §5-A5 行号已更正 |
| F-5 | esc 实为 6 份 esc + 1 份 escHtml | **采纳**。v2 §2/§4.3 措辞已更正 |
| F-6 | 「第一段完全无效」轻微夸大（有重复 error 日志副作用） | **采纳**。v2 §5-A1 改为「构建逻辑完全无效（唯一副作用是重复 error 日志）」 |
| F-7 | P0-2/P0-5 编号与路线图 P1 归类自相矛盾 | **采纳**。v2 §11 增加编号说明（P0-x 是严重度分级、P0/P1/P2 是调度分档），并把 P0-2 留在调度 P0（同评审 B 意见 1），P0-5 顺延 P1 并注明理由 |
| F-8 | batch_service.py 补全目录 | **采纳**。v2 Q-1/Q-9 已写全 `rule_backtest/batch_service.py` |
| F-9 | innerHTML 94→95 处 | **采纳**。v2 §2 已更正 |
| F-10 | scripts 12 个 Python 脚本（另有 deploy.sh/run_dev.ps1） | **采纳**。v2 §0 范围表述已精确化 |
| 总体 | 「需修改后交付（改动量小）」 | 全部修改已落入 v2，进入回签轮确认 |

## 2. 独立审查（independent-review）意见处理

独立审查 67 条编号发现（B1-B16 / S1-S6 / P1-P9 / D1-D9 / A1-A4 / O1-O6 / T1-T5 / F1-F6 / U1-U3 / X1-X3；首轮其收尾回复误报为 45 条，回签轮自纠为 67，本文件与 v2 相关计数已同步修正）与主报告的重合/新增映射：

**与主报告重合（互证，增强置信度）**：B1=P0-1、B4=§5-A1、B6=P0-3（注意：B6 与 v2 的 P0-3 共同犯了「默认 busy_timeout=0」的事实错误，回签轮已修正，见 §5）、B7=C-2、B11=C-20（v2.1 补回，v2 曾误挂为「v1 已含」实际未收录）、B13=S-6、D1=§4.1.2、D2/D3=§4.3、D5=§4.2、D8=§5-B 组、D9=§5-A4/A5、O3=§7-O5、O5=§7-O2、S1=P0-2、S2=P0-4+S-2、S5=S-7、P2=Q-3、P3=Q-2、P7=Q-16、P8=Q-8/Q-14、A1=§4.4-A7、A2=§4.4-A5、T2=§8-5、T3=§8-6、F1=§9-1、F4=§5-B16。

**新增且采纳（全部经主审查逐条核实代码后落入 v2）**：

| 独立审查编号 | v2 落点 | 核实方式 |
|---|---|---|
| B2 空因子擦除 | **P0-7**（升为严重问题，数据完整性） | 精读 service.py:264-272 + provider_tickflow.py:283-297 确认空列表无条件写入与 replace 语义 |
| B3 无建用户入口 | **O-3（中）** | grep `create_user` 调用方仅 tests/，确认 |
| B5 快照永久 None | **C-5（中）** | 精读 dashboard_snapshot.py:49-60，`_snapshot_loaded` 确在 try 前置位 |
| B8 因子 UTC vs K线上海 | **C-4（中）** | 精读 provider_tickflow.py:122-123 vs 294，两套时区换算并存属实；因注释声称已对 vendor 口径验证过，定级「中（隐患）」而非「高（确证 bug）」，建议加回归测试而非直接改口径 |
| B9 批次宇宙/数据版本漂移 | **C-9（低）** | 精读 batch_service.py:386/456 确认标的集合未冻结 |
| B10 月度收益丢首月 | **C-8（低）** | 精读 metrics.py:226-228 确认 pct_change().dropna() 丢首月 |
| B12 用户名大小写 | **C-13（低）** | db.py:1428-1436 确认无 lower |
| B14 变量误命名/F821 注解 | **§5-B11** | ruff 复核确认 |
| B15 _prepare_bars KeyError | **C-14（低）** | engine.py:283-286 确认 |
| B16 last_exit_bar_idx 脆弱 | **C-15（低）** | 确认当前正确但依赖隐含约定 |
| P1 手工交易三重加载 + 双档双算 | **Q-7（中，升级自 v1 的 Q-6/Q-7）** | 精读 trade_records.py:222-233 / manual_trade.py:97 / stop_loss.py:181 确认三次加载；tight/loose 双算确认 |
| P4 _cache_fresh 4 次查询 | **Q-11（低）** | indicator_store.py:169-190 确认 |
| P5 _market_records iterrows | **Q-12（低）** | db.py:1249-1284 确认 |
| P6 _save_backfill_result 重读 | **Q-13（低）** | service.py:504-507 确认 |
| P9 result_full 常驻 + condition_trace 无界 | **Q-15（低）** | rule_backtest.py:176 / engine.py:60 确认 |
| D4 前端块级复制（止损卡已漂移） | **§4.3 表格 + §10-11（产品裁决）** | 抽对 manual_trade.html 与 subject_market.html 止损卡段落，确认两副本存在且细节不同 |
| D6 引擎 MA 手写 | **§4.3 表格** | engine.py:617-619 确认 |
| D7 报价字段映射复制 | **§4.3 表格** | provider_tickflow.py:330-343 vs 398-411 确认 |
| O1 jobs_snapshot 无消费方 | **O-4（中）** | grep `jobs_snapshot` 零调用方，确认 |
| O2 15:00 快照冻结 | **O-9（低）** | scheduler.py:18-24 cron 末轮 15:00 确认 |
| O4 脚本 --db 不一致 | **O-10（低）** | migrate_raw_qfq.py:107 / rerun_daily_update.py:25 确认无 --db |
| O6 部署脚本与文档漂移 | **并入 S-1 + §9-6** | deploy.sh 精读确认 |
| S3 无安全响应头 | **S-3（中）** | main.py 无安全头中间件，确认 |
| S4 会话治理 | **S-9（低）** | services/auth.py 确认 |
| S6 tushare 镜像站通道 | **S-11（低）** | tushare_common.py:20-37 确认 |
| A3 模块级单例/单进程约束无文档 | **§4.4-A6（低）** | 确认 |
| A4 core→data 反向依赖 | **§4.4-A4（中）** | core/jobs.py:17-18 确认 |
| T1 基线红测试 | **§8-9** | CLAUDE.md 自述确认 |
| T4 根目录测试无 marker | **§8-10** | 目录结构确认 |
| T5 fixture 脆弱 | **§8-12** | conftest.py 注释确认 |
| F2 TODO.md 过时 | **§9-3** | TODO.md 确认 |
| F3 架构文档漂移 | **并入 §9-1/2/4** | 确认 |
| F5 仓库混入工件 | **§5-A7** | `git ls-files` 亲核 .coverage/trend-quant.zip/.zip 确认 |
| F6 pyarrow/email-validator 未用 | **§5-B16** | grep 确认 src 无引用 |
| U1 标的无法停用 | **§10-U1（中）** | instruments.html 无 enabled UI + InstrumentUpdateRequest 仅三类目，确认 |
| U2 登录页增强 | **§10-10（低）** | 确认 |
| U3 快照错误未转义 | **§10-8 + §2 XSS 专项例外** | subject_market.html:1092 确认 |
| X1 冗余二级索引 | **C-18（低）** | db.py DDL 确认 PK 与索引重复 |
| X2 raw INSERT OR REPLACE | **C-16（低）** | db.py:1294-1299 确认 |
| X3 新增标的 TOCTOU | **C-19（信息）** | 确认后果可控，文档化即可 |

**部分采纳（调整定级/表述）**：
- B8（因子 UTC）：独立审查定「中」，主审查认为注释声称已对 vendor 全量历史零误差验证过、且本地库长期运行未暴露，故 v2 表述为「隐患 + 建议回归测试」而非「改口径」；
- B16：当前行为正确，v2 定为「低（脆弱设计）」而非 bug；
- S2 中「deploy.sh 明文 HTTP」：并入 v2 S-1 的 deploy.sh 专条统一叙述。

**驳回/不采纳**：无。独立审查 67 条全部在 v2.1 有去向（回签轮确认的 3 项修正——B11 补回、P0-3 事实修正、映射表补 D9/O3/O5——均已落实；其收尾列出的 3 条低门槛残渣——sqlite path 死字段、/login 续期写、/mcp startswith 前缀——也已分别收录为 §5-B15、S-14③、S-14②）。

## 3. 评审 B（覆盖面与建议质量）意见处理

**程序性说明（回签轮更正版）**：评审 B 首轮评审时引用了 `review-1-factcheck.md`、`review-2-arch.md` 等文件，并据此提出「v1 条目在终版无声丢失」的指控。我在 v2 的 §3 中曾断言这些文件「从未存在」——**这个断言是错误的，我写下它时并未实际列出目录**。事实是：本目录在审查期间存在**另一套并行审查**的产物（`code-review-report.md` v2.2 及其 review-1/review-2/blind-audit 系列，由另一个并行会话在 08-25 00:06-00:40 间产出），评审 B 评审我的 v1 时（00:21）这些文件已在磁盘上。评审 B 对「文件存在」的举证（含 review-1 第 227 行的「702 collected」原文）经我列目录核实属实，我向评审 B 致歉并致谢其坚持举证。

同时澄清评审 B 叙事中仍然不成立的部分：那些文件属于**并行会话的审查流程**，与本报告（review-report.md）的 v1 无任何版本沿革关系——我的 v1 从未包含其所称「丢失」的条目，那些条目归属于并行报告（code-review-report.md）自己的修订线。评审 B 在回签轮也接受了这一点（其表述为「如果你们约定 review-report.md 才是 v1……这是命名之争」）。**实质处理不受影响**：其列举的每一条技术事项（copy2 备份、MCP 测试收集失败、dev 依赖、deploy.sh、定时备份、asset_version 前置）均已按实质采纳进 v2/v2.1。

**实质意见处理**：

| 评审 B 编号 | 意见 | 处理 |
|---|---|---|
| A-1/A-2 | deploy.sh 整块未审（HTTP/root/rm -rf/无 .env 步骤） | **采纳**。v2 S-1 单列为「高」；§9-6 记录三处部署描述矛盾；入 P1 |
| A-3 | 30 天滑动续期无绝对上限 | **采纳**，并入 P0-5 |
| A-4 | 登录失败零审计 + 计时侧信道 | **采纳**。失败审计并入 P0-4；侧信道列 S-10（仅记录） |
| A-5 | 无安全响应头 | **采纳**（v2 S-3，与独立审查 S3 合并） |
| A-6 | zip 快照被 git 跟踪 | **采纳**（v2 §5-A7，与独立审查 F5 合并，已亲核） |
| B-1 | 无定时备份机制（高） | **采纳**。v2 O-1（高），含磁盘账建议（keep=3 日备约 8.4GB + 每周异机），入 P1 |
| B-2 | copy2 备份 WAL 活库（高） | **采纳**。v2 O-2（高），已亲核两处行号 |
| B-3 | chinese_calendar 年度边界无运维着落 | **采纳**。v2 O-7（中），含「降级 warning 无人能看见」的联动 |
| B-4 | 带外脚本并发写无互斥 | **采纳**。v2 O-6（中），与 P0-3 联动说明 |
| B-5 | 无 CI | **采纳**，并入 v2 O-8，入 P2 |
| B-6 | pyproject 无 dev 依赖声明 | **采纳**。v2 O-8（中）+ S-12 根因改写，P0 部分（让收集转绿）随 P0-8 |
| C-1 | MCP 测试收集失败（高） | **采纳并升级为 P0-8**。主审查亲测 `--collect-only` 复现 ModuleNotFoundError |
| C-2 | 备份/删除路径缺直接测试 | **采纳**，并入 v2 §8-2 |
| D-1 | foreign_keys 未开启 | **采纳**。v2 C-17（低，开启前需校验存量孤儿行） |
| D-2 | 空目录 + egg-info | **采纳**。v2 §5-A6（已亲核五个空目录与 egg-info 存在） |
| E-1 | asset_version 只跟踪 style.css，是 common.js 的前置 | **采纳**。v2 Q-8 改写并建立与 §4.3 的联动 |
| E-2 | 轮询无可见性感知 | **采纳**（v2 §10-9） |
| E-3 | 止损卡块级漂移需产品裁决 | **采纳**。v2 §4.3 标注「非机械合并」+ §14-2 待裁决 |
| 建议质量 1 | P0-2 应留在调度 P0 | **采纳**（公网可达 + 烧付费配额 + 半天可落地） |
| 建议质量 2 | P0 漏「让测试能跑」 | **采纳**（P0-8 入 P0 首条） |
| 建议质量 3 | busy_timeout 注意带外脚本与 5000ms 校准 | **采纳**（P0-3 与 §7-O6 补注） |
| 建议质量 4 | Q-1 缓存键必须含指标参数 | **采纳**（Q-1 规格注意，防止按错键引入正确性 bug） |
| 建议质量 5 | §4.1 两阶段重构路径 | **采纳**（阶段一纯函数抽取为零风险起点，允许停在阶段一） |
| 建议质量 6 | db.py 全拆降档 | **采纳**（§4.4-A7 降级为 DDL 分段 + users/sessions 域顺手迁出） |
| 建议质量 7 | 备份磁盘账 | **采纳**（O-1） |
| 建议质量 8 | P0-4 路线图显式含 MCP 工具 | **采纳**（P0-4 条目已写明） |
| 建议质量 9 | §8 开头点明 P0-8 前提 | **采纳**（§8 首句） |
| 建议质量 10 | S-2(CSRF) 标「可选」 | **采纳**（v2 S-4） |
| 建议质量 11 | 无恶性过度设计建议（正面评价） | 记录 |
| 结构 1 | 修订追踪缺失 | **采纳**（本文件 + v2 §13）；但其「丢失」叙事不成立（见上） |
| 结构 2 | 编号三层脱钩 | **采纳**（v2 §11 编号说明 + 来源编号列 + 每条必有去向） |
| 结构 3 | 重要发现被埋没（C-2 时区、Q-1、跨进程缓存） | **采纳**。v2 §0 六大问题按「不修会怎样」重排点名；C-2 补「最重的一处是 core/jobs.py:63」；Q-1 入 §0；O-6 与缓存陈旧合并叙述 |
| 结构 4 | §5 死代码分「行为收益/纯卫生」两组 | **采纳**（A/B 组） |
| 结构 5 | P1/P2 缺 Done 定义 | **采纳**（路线图全部条目补 Done 列） |
| 结构 6 | 缺并发设计正面记录 | **采纳**（v2 §7 开头正面清单补充 RevisionCache 单飞、BEGIN IMMEDIATE 等） |

**驳回/不采纳**：
1. 「v1 条目在终版无声丢失」的叙事——不适用于本报告的 v1（相关文件属并行审查流程，见上方更正版程序性说明）；其所列条目本身全部按实质采纳；
2. 其「review-1 实测 702 collected + 2 errors」的数字——该数字出自并行审查的 review-1 文件（真实存在），但主审查自己的实测是「收集即中断」，v2 只写主审查亲测的表述。

## 4. 主审查对三方意见的最终立场（v2.1 更新）

- 三方在核心技术事实上**零冲突**：独立审查与主报告重合的 20+ 条全部互相印证；评审 A 抽查约 50 条论断仅 2 条统计数字失准、1 条引用来源失准（已全部修正）。
- 独立审查的 67 条与评审 B 的 18 条补充，经主审查逐条回代码核实后**全部采纳或合并**（个别调整定级与表述；P0-3 为双方共同事实错误，回签轮共同修正）。
- 评审 B 的「丢失」框架不适用于本报告 v1，但其指出的文件存在性举证成立——主审查此前的否定断言错误，已更正并致谢。
- 回签轮三方结论：独立审查「同意收敛」（3 项修正已落实）；评审 A「可交付」（P0-5 顺延理由已补进 P1 表）；评审 B「可交付」（§0 已点名 Q-1、程序性说明已更正）。
- v2.1 另吸收了**并行审查**（code-review-report.md v2.2）经我方逐条核实的高价值独有发现（C-21/C-22/C-23/C-24/Q-18/Q-19/S-13/S-14/§2 XSS 补充等），两套独立审查在主要发现上高度收敛，互为盲测印证。

## 5. 回签轮记录（round-2）

| 方 | 结论 | 条件/备注 | 落实 |
|---|---|---|---|
| 独立审查 | 同意收敛 | ① B11 补回 → C-20 ✓；② P0-3 事实修正（busy_timeout 默认 5000ms）+ 计数 45→67 ✓；③ 映射表补 D9/O3/O5 ✓；3 条低门槛残渣收录（§5-B15、S-14②③）✓ | 全部落实于 v2.1 |
| 评审 A（事实核查） | 可交付 | P0-5 顺延理由需实际写入 §11 P1 表 ✓；其抽查的 v2 新增 8 组论断全部属实 | 全部落实于 v2.1 |
| 评审 B（覆盖面） | 可交付 | ① §0 点名 Q-1 ✓；② reconciliation §3 程序性说明更正为与磁盘事实一致 ✓ | 全部落实于 v2.1 |

**round-3 最终确认（已闭合）**：v2.1 变更已回发三方签认，结论——

| 方 | round-3 结论 | 备注 |
|---|---|---|
| 独立审查 | **同意收敛**（阻塞项为零，无更多可挖掘点） | 抽核 v2.1 新增 8 条全部属实；Q-19 表述已按其保留意见修正为「返回字典键不一致（helpers 内部已归一化）」 |
| 评审 A（事实核查） | **可交付**（无需修改清单） | 实测复现 busy_timeout 默认 5000；抽核 v2.1 全部新增论断 12 组逐条属实、行号全精确 |
| 评审 B（覆盖面） | **可交付**（无遗留阻塞项） | 认可 reconciliation §3 更正版与其立场一致；18 条补充 + 11 条建议修正 + 6 条结构意见全部闭环 |

**停止条件核验**：① 独立代码审查子代理与主审查完全一致、双方确认无可再挖掘的优化点 ✓；② 两名报告评审子代理与主审查达成一致 ✓。**审查关闭。**
